<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.udea.schedulearning" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".matter.MatterActivity"
            android:label="@string/matters"/>

        <activity
            android:name=".schedule.ScheduleActivity"
            android:label="@string/schedules"/>

        <activity
            android:name=".task.TaskActivity"
            android:label="@string/tasks"/>

        <activity
            android:name=".note.NoteActivity"
            android:label="@string/notes"/>

    </application>

</manifest>
